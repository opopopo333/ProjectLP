<Page x:Class="ProjectLP.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectLP.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CatalogPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#ECF0F1" >
            <TextBox x:Name="TbSearch" Width="200" Margin="5" TextChanged="TbSearch_TextChanged" Tag="Поиск по названию/автору..."/>

            <ComboBox x:Name="CbGenre" Width="150" Margin="5" DisplayMemberPath="Name" SelectionChanged="CbGenre_SelectionChanged"/>

            <ComboBox x:Name="CbSort" Width="150" Margin="5" SelectionChanged="CbSort_SelectionChanged">
                <ComboBoxItem Content="Сортировка: А-Я"/>
                <ComboBoxItem Content="Сортировка: Рейтинг"/>
            </ComboBox>
        </StackPanel>

        <ListView x:Name="LvBooks" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}" 
              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Margin="10" Width="200" Background="White">
                        <StackPanel >
                            <Image Source="{Binding CoverPath}" Height="250" Stretch="UniformToFill" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" Height="40"/>
                            <TextBlock Text="{Binding Users.DisplayName}" FontStyle="Italic" Foreground="Gray"/>

                            <Button Content="Подробнее" Margin="0,10,0,0" Click="BtnDetails_Click" Tag="{Binding Id}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
